server:
  api_url: "http://127.0.0.1:9898/api"
  agent_id: "e865b763-1018-4f3a-bdc6-313fdda28ac5"
  api_key: "54295aae-b6e1-41eb-ac3f-ab824121e1f5"
  heartbeat_interval_sec: 5
  employee_id: "nakul@gmail.com"
  machine_name: "BYTE_BUILDERS"

web_server:
  enabled: true
  host: "0.0.0.0"
  port: 8080
  auto_open: false

database:
  path: "data/agent.db"
  max_open_conns: 1
  max_idle_conns: 1

logger:
  dir: "logs"
  level: "info"
  max_size_mb: 10
  max_backups: 5
  max_age_days: 30
  compress: true

services:
  appTracker: true
  browserTracker: true
  screenshot: true
  input: true
  sync: true
  queue: true

app_tracker:
  poll_interval_sec: 2

browser_tracker:
  poll_interval_sec: 3

screenshot:
  interval_sec: 10
  quality: 80

input:
  poll_interval_sec: 5

sync:
  interval_sec: 5
  batch_size: 50
